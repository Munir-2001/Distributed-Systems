# Stop actors on an unhandled exception instead of restarting them. Akka's
# default restarts with fresh state, which would wipe a replica's positions[];
# our model is crash-stop (replicas don't recover).
akka.actor.guardian-supervisor-strategy = "akka.actor.StoppingSupervisorStrategy"
